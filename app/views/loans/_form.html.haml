= simple_form_for [@user, @loan], url: user_loans_path do |f|
  - if @loan.errors.any?
    #error_explanation
      %h2= "#{pluralize(@loan.errors.count, "error")} prohibited this loan from being saved:"
      %ul
        - @loan.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.input :barcode, as: :hidden

